import { ComponentFixture, TestBed } from '@angular/core/testing';
import { VehiclesPage } from './vehicles.page';

describe('VehiclesPage', () => {
  let component: VehiclesPage;
  let fixture: ComponentFixture<VehiclesPage>;

  beforeEach((() => {
    //fixture = TestBed.createComponent(VehiclesPage);
    //component = fixture.componentInstance;
    //fixture.detectChanges();
  }));

  it('should create', () => {
    expect(true).toBeTruthy();
  });

  it('should create page component', () => {
    expect(true).toBeTruthy();
    //todo: make the rest of the test
  });
  it('should show vehicle list', () => {
    expect(true).toBeTruthy();
    //todo: make the rest of the test
  });
  it('should open vehicle modal', () => {
    expect(true).toBeTruthy();
    //todo: make the rest of the test
  });
  it('should register vehicle', () => {
    expect(true).toBeTruthy();
    //todo: make the rest of the test
  });
  it('should update vehicle', () => {
    expect(true).toBeTruthy();
    //todo: make the rest of the test
  });
  it('should delete vehicle', () => {
    expect(true).toBeTruthy();
    //todo: make the rest of the test
  });
});
