import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TripsPage } from './trips.page';

describe('TripsPage', () => {
  let component: TripsPage;
  let fixture: ComponentFixture<TripsPage>;

  beforeEach((() => {
    //fixture = TestBed.createComponent(TripsPage);
    //component = fixture.componentInstance;
    //fixture.detectChanges();
  }));

  it('should create', () => {
    expect(true).toBeTruthy();
  });
  it('should show trips history', () => {
    expect(true).toBeTruthy();
    //todo: make the rest of the test
  });
  it('should open activate trip modal', () => {
    expect(true).toBeTruthy();
    //todo: make the rest of the test
  });
  it('should activate trip', () => {
    expect(true).toBeTruthy();
    //todo: make the rest of the test
  });
  it('should should cancel trip', () => {
    expect(true).toBeTruthy();
    //todo: make the rest of the test
  });
  it('should should complete trip', () => {
    expect(true).toBeTruthy();
    //todo: make the rest of the test
  });
});
